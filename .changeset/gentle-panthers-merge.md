---
'@kastral/kra': patch
---

Simplify interactive provider setup and refreshes by always merging registry updates, preserving manual `api.json` edits while keeping overwrite available through explicit command options.
